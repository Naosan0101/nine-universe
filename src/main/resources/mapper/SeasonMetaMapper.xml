<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.nineuniverse.repository.SeasonMetaMapper">

	<select id="findLastResetPeriodStart" resultType="java.time.LocalDate">
		SELECT last_reset_period_start FROM season_meta WHERE id = 1
	</select>

	<select id="findLastResetPeriodStartForUpdate" resultType="java.time.LocalDate">
		SELECT last_reset_period_start FROM season_meta WHERE id = 1 FOR UPDATE
	</select>

	<update id="updateLastResetPeriodStart">
		UPDATE season_meta SET last_reset_period_start = #{start} WHERE id = 1
	</update>
</mapper>
